# Project Rules & Preferences

## Git Workflow
- **Active / Target Branch**: Always work on and push code to the `hanzala` branch (`origin/hanzala`).
- Never push directly to `main` unless explicitly requested by the user.
